import LetterAnimation from "../gsap/letter-animation";

export default async function NoResultsFound() {
    return (
        <section>
            <LetterAnimation className="h3 text-foreground" title="no-results-found" />
        </section>
    )
}
